# Python dependencies for the RC-frame Bayesian SHM framework
# Tested with Python 3.11-3.12.
#
# Install with:
#     pip install -r requirements.txt
#
# Notes:
# - openseespy is required only to regenerate the FE dataset from scratch
#   and to run the finite element validation models.
# - The analysis pipeline can be run without openseespy because the generated
#   CSV datasets are included under data/.
# - pgmpy >= 1.0 is required for the Bayesian-network implementation used here.

numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
matplotlib>=3.7
pgmpy>=1.0
openseespy>=3.5.0
